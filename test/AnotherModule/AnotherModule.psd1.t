# Copyright Subatomix Research Inc.
# SPDX-License-Identifier: MIT
@{
    ModuleVersion = '{VersionPrefix}'
    Copyright     = '{Copyright}'
    PrivateData   = @{ PSData = @{ Prerelease = '{VersionSuffix}' } }

    # This file exists only to demonstrate that one project can have multiple
    # .psd1.t files.  When using the test project as a template, it is OK to
    # delete this file.
}

